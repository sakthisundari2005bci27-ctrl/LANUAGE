#include <iostream>
#include <vector>

using namespace std;

int main() {
    // Optimize standard input/output operations for execution speed
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    if (!(cin >> n)) return 0;

    // Use std::vector for safe dynamic size allocation in C++
    vector<int> arr(n);
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    // Traverse the array backwards from index (n-1) down to 0
    for (int i = n - 1; i >= 0; i--) {
        cout << arr[i] << (i == 0 ? "" : " ");
    }
    cout << "\n";

    return 0;
}
